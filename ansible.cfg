[defaults]
hostfile = hosts
ansible_managed = Managed by Ansible. Do NOT edit: {file} by {uid} on {host}


[ssh_connection]
ssh_args=-F ssh.config # It is helpful to create a symlink to ~/.ssh/config
